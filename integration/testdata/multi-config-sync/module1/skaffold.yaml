apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: module1-config
deploy:
  kubectl: {}
build:
  tagPolicy:
    gitCommit: {}
  artifacts:
  - image: test-sync-module1
    context: .
    sync:
      manual:
      - src: '**/sync-file*'
        dest: /
manifests:
  rawYaml:
   - pod.yaml
