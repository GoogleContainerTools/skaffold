apiVersion: "run.googleapis.com/v1"
kind: WorkerPool
metadata:
  name: skaffold-test-wp
  annotations: 
      key: "run.googleapis.com/launch-stage"
      value: "BETA"
spec:
  template:
    spec:
      containers:
      - image: us-docker.pkg.dev/cloudrun/container/worker-pool