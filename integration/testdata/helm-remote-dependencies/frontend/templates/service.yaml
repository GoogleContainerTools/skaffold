apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}
    tier: frontend
  ports:
  - port: 8080
    targetPort: http-server
