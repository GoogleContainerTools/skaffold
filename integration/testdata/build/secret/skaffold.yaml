apiVersion: skaffold/v3alpha2
kind: Config
build:
  local:
    useBuildkit: true
    push: false
  artifacts:
  - image: secret
    docker:
      secret:
        id: mysecret
        src: mysecret.txt
