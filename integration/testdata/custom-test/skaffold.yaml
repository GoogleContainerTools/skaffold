apiVersion: skaffold/v2beta13
kind: Config
build:
  artifacts:
  - image: custom-test-example
test:
  - image: test-dep-files
    custom:
      - command: echo Hello!!
        dependencies:
          paths:
          -  'foo*' 
    # structureTests:
    #   -  'foo*'
deploy:
  kubectl:
    manifests:
      - k8s-*
