apiVersion: skaffold/v3alpha2
kind: Config
build:
  artifacts:
  - image: foo
  local:
    concurrency: 1
profiles:
- name: gcb
  build:
    googleCloudBuild:
      projectId: proj2
      diskSizeGb: 10
      machineType: vm2
      timeout: 180s
      logging: LEGACY
      logStreamingOption: STREAM_ON
      concurrency: 2
      workerPool: pool2
