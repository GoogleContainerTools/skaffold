apiVersion: skaffold/v2beta18
kind: Config
build:
  artifacts:
  - image: test-events
deploy:
  kubectl:
    manifests:
      - k8s-*
profiles:
  - name: custom
    test:
      - image: test-events
        custom:
          - command: echo Hello!
  - name: structure
    test:
      - image: test-events
        structureTests:
          - ./test/*
  - name: customandstructure
    test:
      - image: test-events
        custom:
          - command: echo Hello!
        structureTests:
          - ./test/*
  - name: noimagenames
    test:
      - custom:
          - command: echo Hello!
        structureTests:
          - ./test/*
