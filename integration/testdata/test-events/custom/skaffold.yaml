apiVersion: skaffold/v2beta13
kind: Config
build:
  artifacts:
  - image: custom-test-events
test:
  - image: custom-test-events
    custom:
      - command: echo "bar" > test
deploy:
  kubectl:
    manifests:
      - k8s-*
