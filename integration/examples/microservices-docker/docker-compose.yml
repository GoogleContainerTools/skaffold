
services:
  app:
    image: leeroy-app
    build:
      context: leeroy-app
      args:
        BASE: base
    ports:
    - "9001:50051"
    links:
      - web
    depends_on:
      - base
  web:
    image: leeroy-web
    build:
      context: leeroy-web
      args:
        BASE: base
    depends_on:
      - base
  base:
    image: base
    build: base
    entrypoint: "sh -c"
    command: "exit 0"
