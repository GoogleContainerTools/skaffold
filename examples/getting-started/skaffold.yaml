apiVersion: skaffold/v1alpha2
kind: Config

build:
  artifacts:
  - imageName: gcr.io/dga-demo/skaffold-example

deploy:
  kubectl:
    manifests:
      - k8s-*

profiles:
  - name: gcb
    build:
      googleCloudBuild:
        projectId: dga-demo
