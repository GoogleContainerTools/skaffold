codecov:
  notify:
    after_n_builds: 4

coverage:
  round: up
  status:
    project:
      default:
        threshold: 1%
    patch:
      default:
        threshold: 10%

comment:
  layout: "reach,diff,flags"
  require_changes: yes
  after_n_builds: 4